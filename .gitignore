# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/
.vscode/

# Python (if anyone adds tooling)
__pycache__/
*.pyc
.venv/
venv/
.env

# Local archive (kept out of the published repo)
_archive/

# Obsidian per-user / machine state (the committed config is intentional;
# these are local-only and should NOT be committed)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/
.obsidian/plugins/*/data.json.bak

# Tagging tool derived/scratch state (regenerated by tools/tag_corpus.py).
# Note: tools/overrides.json is the curated reclassification data and IS committed.
tools/.tags_manifest.json
tools/.ambiguous.json
